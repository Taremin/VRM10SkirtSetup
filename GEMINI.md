# GEMINI.md - Geminiとの共同開発ルール

このドキュメントは、このプロジェクトをGeminiと共同で開発していく上でのルールを定めたものです。

## 1. 基本方針

- **目的:** このプロジェクトの目標は「VRM1.0モデルのスカートに、SpringBoneやColliderを自動で設定するUnityエディタ拡張機能を提供すること」です。
- **コミュニケーション:**
    - 指示は明確かつ具体的にお願いします。
    - 作業の意図や背景を伝えていただけると、より精度の高い応答が可能です。
    - 不明な点がある場合は、Geminiから質問することがあります。
- **言語:** 主なコミュニケーション言語は日本語とします。

## 2. 開発ワークフロー

- **要件定義:**
    - 機能追加や大きな変更を行う際は、まず要件を定義し、ドキュメントとして保存します。
    - ドキュメントについて合意が取れた後、実装に着手します。
- **ファイル操作:**
    - ファイルの変更・作成・削除を行う際は、必ず事前に変更内容の確認を求めます。
    - 新しいファイルを作成する場合、既存のファイル構成や命名規則に従います。
- **Git利用:**
    - **コミットメッセージ:** 原則として[Conventional Commits](https://www.conventionalcommits.org/)の形式に従います。メッセージは日本語で記述します。
        - コミット前に`git diff`で変更内容を確認し、その内容を簡潔にコミットメッセージに含めます。
        - 例: `feat: 新機能を追加`
        - 例: `fix: 〇〇のバグを修正`
    - **ブランチ:** `main`ブランチを保護ブランチとし、直接のコミットは行いません。機能追加や修正は、フィーチャーブランチを作成して行います。
        - ブランチ名の例: `feat/new-feature`, `fix/bug-fix`
- **テスト:**
    - コードの変更、特にロジックの変更を行う際は、関連するテストを作成・更新します。
    - テストフレームワークは（例: Unity Test Framework）を使用します。

## 3. コーディング規約

- **フォーマット:** 既存のコードのフォーマットに準拠します。
- **命名規則:** C#の標準的な命名規則（`PascalCase`、`camelCase`など）に従います。
    - 例: `public class MyClass`, `private int myVariable`
- **コメント:**
    - 複雑なロジックや、意図が分かりにくい箇所にはコメントを追加します。
    - `// TODO:` や `// FIXME:` などのタグを適切に使用します。
- **ライブラリ/依存関係:**
    - 新しいライブラリや依存関係を追加する場合は、必ず事前に提案し、承認を得てから行います。
- **変更の範囲:**
    - ファイルへの変更は、その変更が目的とすることに直接関連する範囲に限定します。リファクタリングやフォーマットの修正など、直接関係のない変更を同じコミットに含めないでください。

## 4. その他

- このドキュメントは、必要に応じていつでも更新可能です。変更を提案してください。

---
